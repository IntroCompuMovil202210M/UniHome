<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unihome" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniHome" >
        <activity
            android:name=".chatActividad"
            android:exported="false"
            android:label="@string/title_activity_chat_actividad"
            android:theme="@style/Theme.UniHome.NoActionBar" />
        <activity
            android:name=".Principal"
            android:exported="false"
            android:label="@string/title_activity_principal"
            android:theme="@style/Theme.UniHome.NoActionBar" />
        <activity
            android:name=".Registrar"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>